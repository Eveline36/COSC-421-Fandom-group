Generated via:

```sql
select storyId, tagId from TagLinks join StoryHeaders on storyId=id where storyId in (select id as storyId from StoryHeaders where date > date('2020-03-31') and date < date('2020-07-01')) order by date;
```
